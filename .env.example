# PostgreSQL (local: run `pnpm run db:up` then `pnpm run db:push`)
DATABASE_URL=postgresql://nirogi:nirogi@127.0.0.1:5432/nirogi

# Google AI Studio / Gemini — required for AI chat replies (https://aistudio.google.com/apikey)
GEMINI_API_KEY=
# Optional override (default: gemini-2.0-flash)
GEMINI_MODEL=gemini-2.0-flash
